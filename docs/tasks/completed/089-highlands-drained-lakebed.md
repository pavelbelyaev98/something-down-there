# 089 — Highlands Drained Lakebed Site

**Status:** complete. MainGame retains the Highlands canyon around a contained drained lakebed modelled on the user's references: a wide, irregular unfenced plot of bare pack sediment inside trampled mud, channels winding around it into murky grey-green water, sandy banks, olive islands, muted grass and reeds, and stranded debris, with a soft-glow sun disc, distance-only detail switches, speckle-free foliage and smooth nearby shadows. Presentation/performance refreshes preserve terrain and placement; baked visibility excludes mutable excavation, and depth priming stays off so every MSAA setting renders the same surfaces.

## Objective
Replace the plain round gravel yard with the river section of the approved **Pure Nature 2: Highlands** demo, kept as close to the demo's terrain composition and asset placement as practical, and adapted to the game:
- the river is widened into a former lake that still holds water along the canyon;
- one large section of that lakebed is drained and exposed;
- the existing voxel dig ground sits inside the drained section, ringed by non-diggable ground, rubble and rocks that read as its natural boundary;
- the existing computer, recharge point, return anchor, winch, recovery pads and exhibit stands stand around the dig area.

User direction (playtest iteration): the site is a drained **lakebed**, no longer a drained reservoir. Unrelated systems and areas stay as they are.

## Concept reference
- `00`/`01`: the setting is a drained lake whose falling water exposed valuables and bones; ordinary daylight, no hazards or horror.
- `03` §1–2: one contained worksite; the diggable surface is surrounded by permanent ground that cannot be dug from above, while the saved subsurface grid stays reachable laterally beneath it. Permanent boundaries must look categorically different from diggable ground. No swimming or water interaction. Every opening in the ground is player-made.
- `03` §7 / `09`: near-overhead noon sun, vivid colour grade and underground darkness stay unchanged.
- `07` §1: everything the player needs between trips stays within about ten seconds of the shaft mouth.
- This task rewrites `03` §1–2, `01` §1, `07` §1 and `09` §1 in place: the previous "no mountains, trees, streams or lakes; gravel yard only" rule is superseded by the Highlands lakebed.

## Live codebase analysis
- `SiteLayout` fixes the voxel allocation: 24 × 100 × 24 m at 0.125 m, top at y = 0, round opening radius 12 m. Enlarging it multiplies the ~150 MB density/material footprint and save size, so the dig ground keeps this footprint; the drained section around it is permanent terrain.
- `RoundSiteSetup` owns the site surroundings: removes old environment roots, builds a 256 m closed annular gravel slab (`Surface/Walking apron`, render + collision, `PermanentTerrainBoundary`) and adjusts bedrock walls, grass radius, camera far plane, ground materials and sun. `MainGameSceneBuilder` calls it.
- The slab's bottom face is also the ceiling seen from lateral tunnels beneath the square grid's corners. A replacement must keep a closed underside there; Unity terrain is one-sided.
- Stations: `SurfaceStationSetup` creates the computer only if missing; `SalvageWinchSetup` rewrites winch/pad/stand *local* positions under `Surface/SalvageWinch` on every run; recharge, return anchor and player come from `MainGameSceneBuilder`.
- `ExcavationDaylight` scans renderers under `MainGameRoot` once and adapts only URP Lit materials; vendor BK shaders are untouched. `SurfaceGrassRenderer` clips meadow to `surfaceRadius`.
- `FpsPlayer` resolves dig contracts through `GetComponentsInParent<IDigTarget>`; any collider below a `PermanentTerrainBoundary` reports the permanent-boundary prompt.
- Demo (`Highlands_Demo.unity`): one 2000 m terrain (4097 heightmap, 4096 splat, 2048 detail, 6 layers, 14 detail and 10 tree prototypes, 9174 trees) plus prefab instances grouped as Cliffs, Peaks, BigBoulders, Boulders, Rubble_dense/sparse, Ruins, Trees, Water and Fx. Sea-level water is scaled `Water` planes at y = 10 with a non-trigger MeshCollider (walkable surface); `BK/Water` is back-face culled and world-space mapped. The east river arm runs through a 100–170 m canyon with 10–12 m shelves, 1–5 m riverbed, 40–50 m east cliffs and a waterfall into its southern end.
- Project `TerrainData` assets serialize binary even in Force Text mode.

## Design
- **Section:** a 1000 m window of the demo terrain centred on the east river arm, copied at demo resolution. All Cliffs/BigBoulders/Boulders/Rubble/Ruins/Trees and elevated rivers/waterfalls inside it, plus every demo peak for the horizon, are instantiated as vendor prefab instances with their demo overrides and relative placement. Demo sea-level planes are replaced by a generated lake surface (see below). Birds, butterflies, reflection probes, demo camera, light, volume and environment manager are not copied.
- **Coordinates:** the drained-section dig centre in demo space maps to the MainGame origin; the voxel top stays y = 0.
- **Widened lake:** the canyon floor below the old shoreline is flood-filled from the river. Inside it, the ground falls from the old shoreline through an exposed bathtub band into water, then to a deeper bed. Submerged demo plants, trees and small objects are removed; exposed ones are lowered onto the new ground; large rocks stay.
- **Drained section:** a broad flat of mud, rubble and sparse re-growing grass on the east shelf against the cliff, sloping gently into the lake to the west and the waterfall pool to the south. Its demo trees and lush detail grass are cleared.
- **Dig area:** a flat plateau around the opening. The terrain has a hole over the opening; a draped rim mesh (`Surface/Excavation rim`) covers the terrain-hole staircase with an exact circular edge in the lakebed rubble texture and keeps the closed underside above the grid corners. Rubble patches and rock clusters ring the opening with walkable gaps; the camp side stays clear.
- **Water:** one generated lake-surface mesh with the demo water material covers only ground below the water line, so the voxel pit never contains water. (Iteration 1 replaced the hidden wading floor with play-area walls at the water line.)
- **Stations:** the existing tested cluster stays on the south rim (computer, recharge, return anchor/spawn looking north up the canyon, winch, pads and exhibit stands), lifted onto the lakebed ground. Boundary rocks and rubble keep that arc clear.
- **Presentation:** existing sun, sky and colour grade stay. The demo's exponential haze is enabled and the camera far plane is 3 km. Terrain shadow casting is off.
- **Tooling:** `RoundSiteSetup` becomes `LakebedSiteSetup` (menu *Configure Lakebed Excavation Site*), regenerating terrain data, environment, rim, water and camp from the vendor demo deterministically. The gravel slab and its assets are deleted. Generated assets live in `Assets/Content/Lakebed`; the binary terrain data is stored with Git LFS.

## Edge cases
- Terrain/rim/collider must never cap the circular opening; grass stays within it; the terrain hole never exposes sky from above.
- Lateral tunnels under the grid corners see the rim underside, not through terrain.
- No demo object, water surface or collider intersects the 24 × 24 m grid column or the station footprints; the winch rope clearance above the rim stays free.
- Stations, recharge footprint and spawn stand on flat permanent ground outside the opening.
- Setup reruns rebuild the same environment and do not duplicate objects; vendor assets and the demo scene stay unmodified; the recovery scene is untouched.
- Saves: layout/format of the voxel grid is unchanged, so current saves still load.

## Acceptance criteria
- MainGame shows the recognisable Highlands river canyon with a widened lake, one large drained lakebed section and the dig opening inside it, framed by a natural rubble/rock boundary; stations stand around the opening.
- Digging, pickup, winch recovery, recharge and return still work; the rim and terrain report the permanent-boundary prompt; the player cannot sink under the water.
- Scene/site/terrain/station tests updated to the new ownership and passing; no compiler warnings; Odin validation clean.
- Fresh Windows build at `builds/windows/SomethingDownThere.exe`.
- Concept, baseline, architecture, readme and asset card updated.

## Implementation notes and rejected attempts
- Demo window: 2049² heights at demo spacing (1000 m), 2048² splat, 1024² details; trees, details and objects inside the flooded bed or camp clearance are removed, exposed small objects follow the new ground. Peaks within 1.6 km are kept for the horizon.
- Lake bed: shoreline falls from the old 13 m contour to 1.2 m under water within 5 m, then to 5.5 m. A shallow 1–1.5 m shelf was tried first; the BK water foamed white across it.
- Boundary stones: standing `Rock_*` pillars read as a stone circle and were replaced by lying, half-buried stones at uneven radii. Loose rubble is pushed outward until its bounds clear the opening, so digging never leaves pebbles floating.
- Vendor objects lose *Batching Static*: runtime static batching of ~4,600 LOD objects exhausted system memory on repeated MainGame loads and crashed the Editor during PlayMode tests.
- Terrain shadow casters ignore terrain holes: the collar flickered black/white and the whole shaft would have been shadowed. Terrain shadows are off; cliffs and rocks still cast shadows.
- Camp turned 90° to the cliff side was rejected: the winch recovery physics tests are tuned to the south-rim cable direction and two failed. Several fixtures also assumed a +z spawn heading; `TerrainIntegrationTests.PlacePlayer` and the find-physics setup now start from a fixed heading, and the admin-reset check compares against the return anchor.
- Opening the demo makes Unity auto-upgrade a few vendor files (URP asset, particle materials); those diffs are discarded.

## Verification
- EditMode 288/288 and full PlayMode 224/224 passed; new scene test covers rim/terrain-hole geometry, dig-column clearance, lake/wading floor, batching flags, terrain shadows and station support. No compiler warnings.
- Play Mode review: meadow opening in the drained section, lake to the west, cliffs and waterfall canyon; sunlight reaches a dug shaft; Editor frame ~6–7 ms CPU/GPU at spawn.
- Windows build succeeded after the Odin validation step; the player starts without log errors (~3.7 GB private memory at the title).

## Iteration 1 — playtest feedback
- Feedback: rivers reflected light oddly; FPS dropped; the player could fly high and roam the map; the dig edge needed a clearer, prettier natural transition; UI check requested.
- Reflections: MainGame now uses the demo's sky material (project copy), flat ambient 0.736 × 1, warm sun colour/intensity (angle stays at noon) and a Custom reflection probe with the demo's baked box-projected cubemap covering the canyon. The post profile was already near-identical; the demo's volumetric clouds and wind were not copied (GPU cost; the wind is tuned for the dig meadow).
- Play area: 72 wall boxes along the drained outline (9 m inside its mapped edge, at the water line) and a flight ceiling that stops the feet at 16 m, on Ignore Raycast so aim, digging, lamps and the winch ignore them. The wading floor is gone.
- FPS: draw calls were ~5,700 at spawn, mostly vendor LOD objects and terrain trees/grass. Setup now renders every scenery object in a unique flat colour from 174 viewpoints (grid + edge, ground and ceiling height, black terrain, 2× LOD bias) and removes those never seen (~3,200 of ~4,600), and removes terrain trees without terrain line of sight (~1,900 of ~3,200). The ~150 border pebbles are one merged mesh; terrain pixel error is 3. Draw calls at spawn fell to ~3,200.
- Edge: the rim collar is 0.9 m wide and uses the dig meadow's own material (bare soil ring); a continuous border of small collider-free pebbles marks the circle; a tinted project copy of the Highlands grass layer (`RimMeadow`) plus sparse terrain grass fades raggedly into the mud; station footprints stay grass-free. A lime-green halo with a tinted URP Lit collar was tried first and read as a separate ring.
- Pitfall: creating an asset after `CreateAsset(terrainData)` reloads the unsaved terrain data (heights became zero); the meadow layer is created first.
- UI: title, load-failure dialog (the old Sep 8 editor save is an older format), HUD, computer, pause and settings screens verified in Play Mode after New Game.

## Iteration 2 — water, lighting and world authoring
- Goal: retain the Highlands composition and noon sun, with readable water and shoreline detail instead of white glare. MainGame is the authored scene; the protected recovery snapshot is unrelated to the current environment.
- Diagnosis: MainGame inherited the Mountains grade; URP disabled probe box projection/blending despite the copied probe enabling projection. BK water feeds the already-lit opaque scene back through diffuse lighting, adds foam to that colour, and takes a fractional power of negative foam falloff beyond the shore.
- Architecture: extend `SunPresentationSetup` and `LakebedSiteSetup`, using project-owned water shader/material copies. Composite refraction without lighting it twice, bound foam math, keep normals/reflections, and apply presentation independently of terrain regeneration. Preserve manually tuned copies on later setup runs.
- Edges: lake and elevated rivers share the correction; waterfall materials retain their separate shader. Depth/opaque textures, camera post processing, render-pipeline reflection support and translated baked-probe placement must agree. Never save or overwrite recovery, rebuild terrain for a lighting fix, or lose the normal player pose while reviewing water.
- Acceptance: compare shoreline, open water, elevated river and surface gameplay views; no broad clipped-white water; no new compiler/console errors; existing scene/lighting/terrain checks pass once changes are finished; fresh Windows player. Document terrain/object/material authoring and the destructive scope of site regeneration.
- Verification: same-view original/corrected water comparison, elevated stream and an actual composed gameplay frame after excavation reviewed; no new console errors or warnings. All 288 EditMode and 36 selected PlayMode checks passed. Recovery scene hash is unchanged; MainGame geometry and terrain data are preserved.
- Build cleanup: replace deprecated URP light-loop and GBuffer declarations in the shared BK shaders and project copies, following the installed URP headers. Preserve publisher GUIDs and the project grass MotionVectors pass; its generator correctly rejects the older vendor template. The local Mountains asset card records reimport requirements.
- Delivery: fresh Windows development build succeeds with zero errors and no shader compiler warnings; its only warning is that Pipeline automation is disabled in the player. The full build also reports future pre-baked collision requirements on existing vendor meshes. The standalone player starts and exits cleanly; MainGame is left open with its surrounding terrain selected for authoring.

## Iteration 3 — exterior performance investigation
- Objective: attribute the FPS loss while walking/flying above ground; preserve the accepted canyon/water and the responsive excavation. The player remains inside the drained section and flight ceiling (`03`); stable frame pacing and readable silhouettes remain required (`09`, `14`).
- Analysis: the existing generator already removes scenery unseen from the whole play volume. It retains ordinary LOD prefab instances, terrain trees/details, a static reflection cubemap and the existing game renderer; unrestricted exploration and live reflection capture are not required.
- Measurement architecture: extend the opt-in `SurfacePerformanceFixture` validation player with fixed ground, shore and flight views, uncapped frame/GPU/CPU timing and render counters. Compare one environment group/setting at a time, repeat baseline views, and write reports outside game assets. No save ownership, normal-scene component or persisted scene/material changes.
- Edges: distinguish CPU submission from GPU and frame-cap waiting; warm each variant; keep resolution, camera, device preferences and population consistent; restore each override before the next comparison. Disabling whole groups is diagnostic evidence, not an approved visual simplification.
- Acceptance: identify the largest measured costs, quantify useful reversible candidates and explain view-volume-specific options and visual trade-offs. Do not claim old FPS is restored from Editor timings or change the accepted world based only on object counts.
- Initial measurements: native Direct3D 12 on RX 9060 XT / Ryzen 7 9700X at 2560×1440. Repeated baseline GPU medians were about 12.3–12.6 ms at the dig site, 7.6 ms at the shore and 11.9 ms in flight; CPU main work about 2–2.6 ms and render-thread work about 0.5 ms. The early run's focus callback restored the device frame cap, and the additive fixture omitted scene lighting/occlusion. These were preliminary attribution results; iteration 4 replaces the environment boot with a single-scene copy and repeats old/new quality at consistent lighting, with isolated preferences and cap validation.
- Attribution: removing sun shadows saves roughly 2 ms in the expensive views; rock scenery, terrain surface and terrain trees each save roughly 1–2 ms; water saves roughly 0.7–1.2 ms, and terrain details less. These savings overlap. Disabling environment colliders does not materially improve timing. Static reflections do not render a second camera. Native draw/batch counters returning zero are unavailable, not evidence of zero work.
- Direction: test earlier scenery LODs, simpler distant terrain shading/geometry, restrained terrain foliage and existing shadow/MSAA quality options together. Further savings should come from cheaper distant backdrop representations and view-dependent occlusion for the bounded play/flight volume; the scene has no baked occlusion data. Keep mutable excavation out of static occluder data and preserve canyon silhouettes, near-ground detail and water. GPU Resident Drawer is a later CPU-submission option, not the leading remedy for the measured GPU limit ([Unity requirements](https://docs.unity.com/en-us/engine/6000.5/manual/analysis/graphics-performance-profiling/in-urp/reduce-draw-calls-urp/reduce-rendering-work-on-cpu/gpu-resident-drawer)).
- Validation: the attribution pass covers three fixed views and repeated baselines; a fully hidden follow-up correctly rejects missing GPU timings, so combined candidates remain unmeasured. All 288 EditMode and 8 startup checks pass. The diagnostic player and normal Windows player build successfully; the investigation adds no component or quality override to MainGame. Recovery and device-preference hashes are unchanged.

## Iteration 4 — contained-world rendering budget
- Objective: apply the approved exterior optimizations and retain smooth nearby shadows rather than blocky low-resolution edges. Concepts `03`/`09` now allow earlier backdrop detail reduction and removal of distant shadows; playable ground, flight silhouettes, water, mutable excavation and lamp occlusion remain authoritative. Stable frame pacing follows `14`.
- Analysis: the native comparison is GPU limited. Global LOD bias doubles vendor detail; several tree variants retain dense geometry at very small screen sizes. Terrain shades seven layers far outside reachable ground. Sun filtering uses the expensive high-quality kernel and a large cascaded map. Runtime preferences reserve the complete lamp range, so simply reducing the authored shadow distance cannot shorten the sun range independently.
- Architecture: extend `LakebedSiteSetup` with a performance refresh that preserves placement, terrain heights/paint and water. Tune terrain detail/shading distance and project LOD bias, create project-owned tree variants with earlier final LODs, and omit scenery shadow casters safely beyond the playable footprint. `SunPresentationSetup` owns a smaller two-cascade sun map with soft filtering; `UnityGameSettingsPlatform` keeps distinct quality tiers and the lamp range. Keep existing explicit MSAA preferences; reduce the fresh/reset default if the combined comparison supports it.
- Occlusion: bake only solid permanent scenery as occluders, with camera volumes covering the play/flight area and the entire excavatable column. Never bake the mutable ground, its edit-mode preview, terrain hole, foliage or water as solid occluders. Keep static batching disabled. Authoring documentation must explain rebaking after scenery edits.
- Edges: repeated refresh must not compound LOD thresholds, duplicate tree prototypes, alter instance placement or overwrite vendor assets. Scene regeneration must apply the same settings and invalidate stale occlusion. Retain nearby cliff casters conservatively, and preserve shadow filtering on reduced tiers. Benchmark overrides remain disposable and never write device preferences.
- Acceptance: native same-view baseline/candidate comparisons with valid GPU timings and fixed resolution, ground/shore/flight visual review plus a dug shaft; retained canyon silhouettes and readable soft nearby shadows; warning-free compilation, relevant checks once at the end, clean validation and a fresh normal Windows build. Record measured improvements without adding overlapping attribution savings or promising 144 FPS everywhere.
- Implementation: terrain and scenery detail are reduced conservatively; the aggressive terrain preset was rejected for visibly flattening the distant ground. Project tree variants switch dense intermediate meshes earlier without shortening their visible range. Sun uses a smaller two-cascade map with the 5×5 soft filter; all tiers retain soft shadows and the shared lamp range. Graphics reset/new preferences use 2× MSAA, while existing explicit preferences remain intact.
- Visibility validation: enable Unity's built-in Umbra module, then bake solid canyon rocks only. The environment benchmark boots a temporary MainGame copy with its save controller removed, preserving scene lighting and visibility data; other validation modes retain their additive save-fixture flow. A runtime previous-quality reference restores vendor trees, original LOD thresholds, terrain settings and sun quality at the same placements. Prototype tables are replaced with an empty temporary population before restoring remapped instances.
- Authoring audit: repeated performance refresh produces the same prototype/caster/LOD state. Before/after hashes of heights, paint, all tree placements and scenery transforms match; recovery and persisted device preferences remain untouched. Occlusion adds only a small view-dependent saving, so it is not credited with the main performance gain.
- Native result: the clean single-scene comparison retains all terrain trees and reports valid uncapped GPU timing at 2560×1440 on RX 9060 XT. Previous-quality reference → optimized existing 4× MSAA: about 81→103 FPS at the dig site, 83→121 in flight, 129→175 at the shore. With the new 2× MSAA default: about 115/137/198 FPS respectively. Repeated reference and optimized baselines remain stable; this does not establish a universal 144 FPS floor. Reports/images are in ignored `unity/Logs/EnvironmentReview/Final/`.
- Visual check: ground, shore, flight ceiling, camp shadow contacts and cliff boundary preserve the canyon and approved water. Eight actual excavation cuts verified sunlight, soil, finds and rooted meadow from inside a newly opened shaft with culling enabled, using a disposable profile.
- Regression verification: all 288 EditMode checks and 59 selected PlayMode checks pass (graphics/UI, startup, terrain interaction and excavation daylight). The clean native benchmark exits normally without errors or warnings, and retains the same tree population in every comparison. No C# or shader compilation warnings remain.
- Delivery: the fresh normal Windows player builds successfully with zero errors. Only the existing vendor collision pre-bake advisory and intentionally disabled Pipeline player automation warnings remain. MainGame is open with its surrounding terrain selected; recovery and device preferences keep their original hashes.

## Iteration 5 — playtest visual fixes
- Sun: the demo's `_SunSize` 0.012 / convergence 10 read as a pinpoint star. `SunPresentationSetup` now applies 0.05 / 4 to the project sky copy: a clear disc with a soft glow (`03` updated).
- White lake sheet: the vendor `Watersplash.mat` references base/normal textures absent from the pack, so the waterfall splash's horizontal billboards (up to ~85 m) rendered as solid white quads on the south lake. `ConfigureWater` removes those particle instances; the scene test rejects untextured environment particles. Other stale texture references on the water materials are unused shader properties.
- Rock popping: LOD bias 1 moved each rock's coarsest mesh (e.g. 601 of 12k cliff triangles) to twice the old distance; it flattens relief and visibly shifts the layered moss mask. Intermediate LODs are visually close and keep the cheaper switch; the coarsest mesh and cull threshold keep the former 2× distances, and every scenery LOD group cross-fades (all scenery shaders support `LOD_FADE_CROSSFADE`). Occlusion was rebaked after the refresh.
- MSAA Off: URP only primes depth without MSAA; the forced priming's equal-depth opaque pass dropped the runtime excavation ground, showing sky through the terrain hole. Depth priming is disabled in `SurfaceGrassSetup`, so every anti-aliasing option takes the same path. The 2× default and MSAA choices are unchanged.
- Verification: same-view captures before/after for the sun, the south-lake sheet, forced LOD levels and MSAA Off at the dig site and shore; no console errors. 288 EditMode and 134 selected PlayMode checks pass; the Windows player builds with only the existing collision pre-bake and Pipeline-disabled advisories.

## Iteration 6 — distant-only detail and clean foliage
- Playtest: rocks, bushes and trees still switched detail within about 10 m; far trees looked flat, and mid-distance bushes and trees were dotted and pixelated.
- Causes: at LOD bias 1 the vendor thresholds switch small boulders at ~5 m and bushes at ~11 m. The iteration 4 tree variants started flat impostors at ~100–130 m (vendor: ~190–840 m at bias 2). Vendor tree LOD groups cross-fade over half of every band, and the leaf/impostor materials' Hide Sides dither edge-on cards with an 8×8 Bayer pattern across a wide angle band. Both left permanent speckle.
- Rejected: turning Hide Sides off made canopies solid but showed smeared edge-on cards up close; the iteration 5 partial fix (coarsest mesh only) still switched near the player.
- Implementation: the performance refresh converts every environment LOD group and project tree/bush variant from vendor thresholds to camera-distance floors (`DetailSwitchDistances` 50/80/120 m; coarsest mesh or impostor ≥ 200 m) at the widest permitted FOV (90°), sized by the largest scale axis. Farther vendor switches and the halved cull are kept. Every switch uses a timed 0.5 s cross-fade where all shaders support it. Project copies of the foliage materials (`Content/Lakebed/Trees/Materials`) raise `_HidePower` from 2.5–3 to 8, keeping cards hidden over a narrow band. At the default 75° FOV, trees and bushes first switch at ~65 m and become impostors beyond ~260–440 m.
- Benchmark: the environment fixture's previous-quality reference now restores recorded vendor LOD thresholds instead of multiplying the current ones. Frame rate was not re-measured natively for this iteration.
- Verification: same-view 1440p crops before/after for a ~15 m and a ~100 m tree and nearby bushes; occlusion rebaked. 288 EditMode (after correcting the new scene test to use the largest scale axis) and 134 selected PlayMode checks pass; the Windows player builds with only the existing collision pre-bake and Pipeline-disabled advisories.

## Iteration 7 — believable drained lakebed
- Playtest: the drained section read as an artificial flat brown yard. Reference: cracked dry mud, darker wet silt and pebble beaches toward the water, trickling channels, reeds, stones and debris. User decisions: keep the dig meadow; add shallow trickling channels (`03` updated).
- Assets: no approved pack has cracked mud, silt or driftwood (inventory of Highlands/Mountains/Common). The crust is therefore a project-generated tileable texture (`art/lakebed-crust`); reeds, rushes, feather grass, pebbles and twigs come from Mountains detail prototypes; rubble and small rocks from Highlands, with a bare-rock material copy.
- Implementation (`LakebedSiteSetup.Lakebed.cs`): Catmull-Rom courses with faded meanders carve monotonic beds into the drained floor outside 31 m (dry gully first); inland beds stay just above the lake plane and only mouths drop into it. The lake mesh covers uncarved lakebed only, testing every sample (corner-only tests left straight gaps). Walkable trickle ribbons with no collider end over each mouth. Painting uses channel distance and height above water: crust patches, silt, stony dark beds (no pale sand under water), strand gravel and waterline sand. Eight terrain layers keep two splat passes. Hummock noise breaks up the flat floor.
- Rejected: water with a short depth fade (`_Depth` 0.1) or pale sand beds read as a saturated blue canal; a long fade over sand vanished. Dark stony beds with `_Depth` 0.12 read as clear shallow water. Carving the dry gully after the wet channel left a round pool under the stream.
- Editor note: `SaveMesh`'s `CopySerialized` update did not refresh an existing mesh's GPU buffers in the running Editor (the old trickle stayed invisible until the geometry was rewritten); it now also rewrites the vertex/index data through the Mesh API.
- Verification: same-view captures of the overview, channels, mouths, spawn meadow and west shore; occlusion rebaked. `MainGameSceneTests` guards trickle/debris clearance and trickle material; small stones keep full detail until their vendor cull. Frame rate not re-measured natively.

## Iteration 8 — look comparison for crust and dig edge
- Playtest: the cracked crust read as artificial; the dig zone read as a perfect circle: a reddish soil collar and continuous pebble ring, dark Mountains turf/plants inside, lime Highlands grass outside. User request: several variations toggled from the menu.
- Causes: the collar mesh has no material-weight channel, so the ground shader treats it as clay/rock soil; the rim meadow is a tinted Highlands grass layer with Highlands detail grass; crust borders all crack, forming a uniform polygon mesh with dark outlines.
- Implementation: `LakebedLook` (runtime, development admin only, session state) cycles four ground and four edge options. Terrain edits go to a lazily instantiated copy of the terrain data (the collider keeps the authored data): crust/meadow layer swaps, a regional repaint within 42 m (old rim meadow moved to mud, then an irregular noise-edged meadow patch) and regional thinning of canyon detail grass inside 24–32 m. `SurfaceGrassRenderer` accepts a per-layer density function and an apron of the same meadow species rooted on the terrain, drawn unclipped in separate batches and excluded from station footprints; the default placement sequence is unchanged. A session mesh copy with zero material weights turns the collar into turf; regrowth uses a `TerrainVolume` presentation material with the pack mud as turf.
- Assets: soft cracks derive from the Highlands mud textures with per-border crack dropout (the first attempt, cracking every border, still read as the same polygon mesh). Packed sediment tint was warmed after the first capture read grey-green. Scattered stones were enlarged after the meadow hid 15–40 cm stones.
- Measurements (Editor, overview camera): seamless/scattered edges roughly double meadow clumps (651→1342, 11→22 draws, +0.2 ms submission); regrowth halves them. Switching costs 20–90 ms after a one-time terrain copy.
- Verification: same-view captures of every ground and edge option; `TerrainIntegrationTests` guards the menu buttons, runtime-copy isolation and exact restoration of authored terrain paint/details, collar mesh, ring and soil material.

## Iteration 9 — reference plot, pack ground and silty water
- Playtest: keep packed sediment and drop the other variations and all custom textures; no grass in the dig; a wider, non-circular dig area; bring the rest closer to two reference images (fenced irregular plot, channels winding around it, sandy banks, grassy islands with rocks, grass tufts); water less blue and muddier (`03`/`09` updated).
- Layout: the grid widens to 36 × 26 m, still inside the save sample bound. `SiteLayout.OpeningRadius(compass)` is a lobed east-west ellipse blended to the tested 12 m circle over the camp arc, so stations, winch and spawn keep their places. Hole, collar, lake, dressing and station checks use `BeyondOpening`; the collar follows the outline and roofs the rest of the grid; `PlaceBedrock` sizes the walls from `SiteLayout`.
- Removed: generated crack/crust textures, the look comparison (`LakebedLook`, variants partial), the pebble ring, the rim meadow layer and the dig meadow with its renderer, shader, validator, tests and materials. `NatureEnvironmentSetup` keeps the BK wind manager and URP settings the old grass setup owned.
- Ground: the Mountains gravel tinted as packed sediment is a terrain layer; the plot cap shares its texture, tile and world alignment. The plot is slightly paler than a dark trampled-mud band outside the fence; a darker cap was tried first, but the references show a lighter plot.
- Fence: one generated post-and-rail mesh in bleached Highlands ash bark on Ignore Raycast, open over the camp arc.
- Channels: rerouted around the plot. Carving fades out before the collar; a stream over a grid corner exposed the collar roof as a dark slab. The lake mesh now follows carved ground and each trickle runs out flat over its flooded mouth one render queue before the lake. Testing only uncarved ground left square lake cells across the mouths; letting the lake draw over a trickle doubled the water into a dark patch.
- Water: lake, river and trickle share one silt setting (slate grey-green, `_Depth` 0.3) so trickles meet the lake without a seam. Rejected: fades of 1.4–1.8 read as clear mint water; darker colours read olive-black.
- Islands: plateau profile with grassy tops over sand rims; the larger ones carry a grass-topped boulder. Plants: canyon `Grass_3`/`Grass_4` as olive dry-grass prefab variants, since the BK grass shader ignores terrain detail colours.
- Finds: the wider grid spread the unchanged catalog about 40% thinner (the fresh-dig density test failed), and scaling counts would exceed the population bound. Placement now draws find centres only beneath the plot outline (`SiteLayout.FindFootprint`), keeping the accepted average density; a half-metre margin left too many sparse patches, and a few seeds' sparsest decile now holds five fresh finds instead of six; the starter finds and the authored uniques keep their previous world positions (catalog positions shifted with the grid corner).
- Tests: seam, spike, boundary and material-strip checks derive their positions from `SiteLayout` (the odd north-south chunk count leaves the centre mid-chunk); the dig-column check uses the plot's inscribed circle.
- Not in the approved packs: the references' excavator and tent camp.
- Editor note: deleting a terrain-referenced material on disk while the Editor ran crashed it in the detail renderer; delete assets through the AssetDatabase.

## Iteration 10 — no fence, muted grass
- Playtest: remove the fence; the grass is too bright.
- The fence, its mesh and `FenceWood.mat` are deleted; the plot reads through its paler cap and the trampled-mud band. Dressing keeps the same clearance from the outline (`DressingClearance`).
- Cause of the lime tufts: `DryGrass.mat` inherited full smoothness from the Highlands grass, glaring under the noon sun, and the Mountains rushes and reeds rendered their vendor materials at a white tint. All three now use darker olive/sage project copies with smoothness 0.1 (`DryVariant`); vendor materials stay untouched.
- Island tops and higher flats paint `DryTurf`, a copy of the grassy mud layer with a (0.6, 0.5, 0.4) diffuse remap: even scaling kept the texture lime and a stronger hue shift reddened its mud. It is the eighth terrain layer, so the terrain keeps two splat passes. The canyon beyond and boulder moss keep the pack look.
- Documentation gap: the fence was generated art recorded only as a line on the Highlands card. `AGENTS.md` now requires a card for original generated art in the same change that creates it (pack copies stay on the pack card), and the lakebed's generated terrain, rim and water meshes have their own card (`art/lakebed-site`).

## Iteration 11 — natural blend
- Playtest: stones floating over channels, a water sheet cutting through a boulder and hanging over the beach, a green smear and white line in the water.
- Floating: rubble clusters and demo rock piles sat at the ground height under their centre, so channel cuts beneath one side left stones in the air (9 of 151 checked in the play area). Debris now sits on the lowest ground under its footprint and clusters over uneven ground are left out; demo scenery follows its smallest ground change and is only dropped when it would end up mostly buried (dropping every unevenly reshaped object removed about 120 demo rocks the drained floor had merely raised). A footprint check now finds none floating.
- Water: the fixed-width ribbon floated over low banks, cut through boulders and ended in straight foam lines. Stream water is now one grid of terrain-sample cells wherever the carved bed lies below the stream's surface (edges stay under the banks), with the lake's own material. Courses are pushed around the demo's boulders and carving fades out before them. The bed floor eases into the lake instead of dropping in one step. On the low shore the surface eases to 5 mm above the lake plane; ending it short of the lake left a seam, and tilted cells over the beach read as pale wedges. Grasses stay out of the water.
- The green smear was refraction of the lime banks; refraction is now 0.1 (lake) and 0.06 (streams). Softer foam made no difference to the shoreline and was reverted.
- Grass: the lime lawn around the drained section and the moss-topped boulders were the brightest remaining greens. `DryTurf` now uses the Mountains `Grass01` layer tinted olive (the Highlands grassy mud stayed lime under any per-channel tint and reddened its mud), and the canyon's vivid grass layers fade into it on low ground near the drained section, leaving the cliff tops. Boulders standing in the old lake or on the drained section are bare rock.

## Iteration 12 — water edges
- Playtest: stream water floating in stair-steps over sand, polygonal water patches over flat ground.
- Causes: stream cells at the edge of a course's reach kept the water level above dry ground; channel beds dipped below the lake plane wherever one bank was low (the previous mouth ease cut them up to 1.2 m) and rose again, leaving inland pools the flat lake plane under the drained flats showed through; each ground sample took its bed from the single nearest course stretch, which jumps at bends.
- Fixes: beds stay 3 cm above the lake until the course reaches open lake water, then fall at most 8 cm per metre; carving takes the deepest cut of every stretch in reach and the stream level blends nearby stretches; corners at the edge of the stream grid dip under the ground; the lake covers only uncarved ground and deep mouths. The south stream now rises west of the boulders instead of among them. Checks: no stream-edge vertex above the ground, no isolated below-lake ground on the play area, no floating debris.
- Rejected: capping the stream surface below both bank tops dried most streams (low banks at the flats); full terrain detail did not change the outlines, so terrain LOD was not the cause.
