namespace SomethingDownThere
{
    public enum DiscoveryKind { Common, Unique }
    public enum RecoveryMethod { Bag, Rope }
    public enum FindState { World, Extracting, Collected, Stored, Displayed }
}
